/**
 * Deprecated: the full-bleed "WOL" wordmark reveal has been removed from
 * the footer for a cleaner close. This component is kept as a no-op so any
 * existing import doesn't break the build — safe to delete this file and
 * its import once you've confirmed nothing else references it.
 */
export function FooterWordmark() {
  return null;
}